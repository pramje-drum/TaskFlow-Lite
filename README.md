# TaskFlow-Lite

- Making the Header component for the project which will be use in both login page and dashboard.
- Making the login poage with the help of the react hook form
- json-server --watch db.json --port 3000 ./cors.js